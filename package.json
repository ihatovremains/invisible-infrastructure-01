{
  "name": "invisible-infrastructure-01",
  "version": "1.0.0",
  "private": true,
  "description": "An interactive visualization of the invisible system behind an accountable enterprise AI answer.",
  "scripts": {
    "serve": "python3 -m http.server 8000",
    "render:smoke": "./render-video.sh --smoke --clean",
    "render": "./render-video.sh --clean",
    "encode": "./render-video.sh --encode-only"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
